# GitHub personal access token (opcional)
# Sin token: 60 req/h por IP — riesgo de rate limit en builds
# Con token: 5000 req/h — recomendado para producción
# Generar en https://github.com/settings/tokens (classic, sin scopes)
GITHUB_TOKEN=
