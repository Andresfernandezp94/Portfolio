---

---

<section class="pt-40 pb-20 px-6 max-w-6xl mx-auto">
    <div class="max-w-5xl">
        
        <h1 class="text-5xl md:text-7xl font-bold leading-tight mb-6">
            Andres<br />
            Fernandez
        </h1>

        <p
            class="text-xl font-bold tracking-wider uppercase mb-4"
            style={`color: var(--color-accent);`}
        >
            Data Platform | Cloud Architect | BI Solutions
        </p>

        <p
            class="text-lg md:text-xl leading-relaxed "
            style={`color: var(--color-text-secondary);`}
        >
            Convierto datos en sistemas visibles, inteligentes, automatizados y  escalables. 
            
        </p>

        <p
            class="text-lg md:text-xl leading-relaxed"
            style={`color: var(--color-text-secondary);`}
        >
            Construyo ecosistemas de datos sobre arquitecturas cloud y on-premise end-to-end.
            
        </p>

        <p
            class="text-lg md:text-xl leading-relaxed mb-10"
            style={`color: var(--color-text-secondary);`}
        >
            Transformo necesidades de negocio y procesos complejos en decisiones rápidas y accionables.
        </p>
<!-- 
        <div class="flex flex-wrap gap-4">
            <a
                href="#proyectos"
                class="px-6 py-3 rounded-lg text-sm font-medium text-black transition-colors"
                style={`background-color: var(--color-accent);`}
            >
                Ver Proyectos
            </a>
            <a
                href="#contacto"
                class="px-6 py-3 rounded-lg text-sm font-medium transition-colors"
                style={`border: 1px solid var(--color-border); color: var(--color-text-secondary);`}
            >
                Contáctame
            </a>
        </div> -->
    </div>
</section>
