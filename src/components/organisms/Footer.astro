---
const year = new Date().getFullYear();

const links = [
    { label: "GitHub", href: "https://github.com/Andresfernandezp94" },
    {
        label: "LinkedIn",
        href: "https://www.linkedin.com/in/andresfernandezp94",
    },
    {
        label: "Email",
        href: "mailto:andresfernandezp94@gmail.com",
    },
];
---

<footer class="w-full fixed bottom-0 left-0 right-0 z-40 border-t border-border bg-bg/90 backdrop-blur">
    <div
        class="absolute inset-x-0 top-0 h-px bg-gradient-to-r from-transparent via-accent/60 to-transparent"
        aria-hidden="true"
    ></div>
    <div
        class="max-w-5xl mx-auto px-6 sm:px-8 lg:px-10 py-4 flex flex-col md:flex-row md:items-center md:justify-between gap-3"
    >
        <div
            class="flex flex-wrap items-center gap-x-3 gap-y-1 text-sm text-text-muted font-mono"
        >
            <a href="/" class="text-base font-bold text-text">
                AF<span class="text-accent">.</span>
            </a>
            <span>© {year} Andres Fernandez</span>
        </div>

        <nav
            aria-label="Links secundarios"
            class="flex flex-wrap gap-x-6 gap-y-1 text-sm"
        >
            {
                links.map((link) => (
                    <a
                        href={link.href}
                        class="text-text-secondary hover:text-accent transition-colors"
                        rel={link.href.startsWith("http") ? "noopener noreferrer" : undefined}
                        target={link.href.startsWith("http") ? "_blank" : undefined}
                    >
                        {link.label}
                    </a>
                ))
            }
        </nav>
    </div>
</footer>
